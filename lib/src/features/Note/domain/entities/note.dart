class Note {
  int id;
  String note;
  String title;
  String backgroundColor;
  // DateTime date;

  Note({
    required this.backgroundColor,
    required this.id,
    required this.note,
    required this.title,
    // required this.date,
  });
}
